export type Theme = '2d' | '3d';

/**
 * Global theme toggle for the renderer.
 *
 * Update this value to switch between the classic 2D canvas renderer and
 * the stylised 3D-inspired renderer.
 */
export const theme: Theme = '2d';
